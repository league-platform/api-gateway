# API Gateway Environment Variables Example

# This is an example of the environment variables needed for the API Gateway project.
# You can copy this file to .env and customize the values according to your environment.

# NGINX configuration
NGINX_HOST=localhost
NGINX_PORT=80

# Backend services
SERVICE_1_URL=http://service1:8080
SERVICE_2_URL=http://service2:8080

# Environment settings
ENVIRONMENT=development

# Logging
LOG_LEVEL=info

# Database configuration (if applicable)
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=database_name

# Other configurations
API_KEY=your_api_key_here
SECRET_KEY=your_secret_key_here